<div class="container">
  <%= form_for @user do |f| %>

    <% if @user.errors.any? %>
      <div id="error-explanation">
        <div class="alert alert-danger">
          The form contains <%= pluralize(@user.errors.count, "error") %>.
        </div>
        <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li>* <%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group row">
      <h4>What industries are you interested in? </h4>
      <div class="col-md-8">
        <% Industry.all.sort_by(&:name).each do |industry| %>
          <% checked = current_user.industry_interests.pluck(:industry_id).include?(industry.id) %>
            <div class="checkbox">
              <label style="font-weight:normal">
                <%= check_box_tag("industry_interests[]", industry.id, checked) %> <%= industry.name %>
              </label>
            </div>        
        <% end %>
      </div>
    </div>

    <div class="form-group row">
      <h4>Please write a short summary that describes you (example: Product Manager with an interest in web and mobile development). Users that include a summary are 40% more likely to connect with other professionals.</h4>
      <%= f.text_area :summary, class: 'form-control' %>
    </div>

    <%= f.submit "Submit", class:"btn btn-primary" %>
  <% end %>
</div>
