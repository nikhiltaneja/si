Hi <%= @first_user.first_name %>,

We found someone that you should consider meeting in the <%= @second_user.location.area.split(",").first %>.

<% if @shared_connections_count > 1 %>
  You have <%= @shared_connections_count %> shared connections on LinkedIn.
<% end %>

<%= @second_user.name %>
<%= @second_user.headline %>

It will take less than 10 seconds to decide if you would like an intro, so click on the button below!

<%= link_to "See #{@second_user.first_name}'s Profile", user_match_url(@first_user, @first_user.current_match) %>


-The SwiftIntro Team
